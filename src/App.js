import React, { Component } from 'react';
import Trampos from './components/trampos';

class App extends Component {
  render() {
    return (
      <div className="App">
       this works, so it seems...
       <Trampos />
      </div>
    );
  }
}

export default App;
